! Web Playerの配置

 $$Web Player$$ でビルドをする時, 自動的にHTMLファイルが同じ階層に生成されます。
 そのHTMLの中にはビルドしたWebPlayerデータをロードするコードが含まれています。

それだけではなく、HTMLコンテンツを追加したり、デザインの変更など自由に生成するHTMLファイルをカスタマイズすることが可能です。カスタマイズについて以下のページで説明します。

(:tocportion:)

